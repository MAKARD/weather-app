import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  temperatureText: {
    fontSize: 40,
    fontWeight: '400',
    color: '#FFFFFF',
    flex: 1
  },
  description: {
    fontSize: 16,
    color: '#FFFFFF'
  }
});
